[package]
name = "tfhe-bridge"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]  # ← THIS is the key

[dependencies]
tfhe = { version = "1.3.1", features = ["integer"] }
jni = "0.21"
bincode = "1.3.3"
